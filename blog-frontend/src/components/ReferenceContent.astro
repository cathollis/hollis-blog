---
interface Props {
  index: number;
}

const { index } = Astro.props;

const referenceLink = `#reference${index}`;
const contentName = `reference-content${index}`;
---

<a href={referenceLink} name={contentName}> [{index}]</a>

<style></style>
